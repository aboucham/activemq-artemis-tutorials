= Step-by-Step Tutorial: Filtering AMQ Broker Metrics using the Built-in JMX Exporter on OpenShift

This tutorial provides a step-by-step guide to configuring a highly customized metrics architecture for *Red Hat AMQ Broker on OpenShift Container Platform*.

By default, the AMQ Broker Operator provides an internal Prometheus plugin that exports a wide baseline of metrics[cite: 168, 3240, 3243]. However, to dramatically reduce metrics noise and storage footprints, this tutorial demonstrates how to leverage the container image's built-in **Prometheus JMX Exporter Java Agent** (`/opt/agents/prometheus.jar`) alongside an OpenShift secret to precisely whitelist only your high-value metrics targets (such as address sizes and consumer counts).

== Prerequisites

* [cite_start]A running OpenShift Container Platform (OCP) cluster[cite: 25].
* The AMQ Broker Operator deployed in your namespace[cite: 25].
* Cluster administrator privileges or standard deployment privileges in the designated project[cite: 315, 316].
* [cite_start]A local workspace configured with the `oc` command-line interface (CLI)[cite: 115].

== Step 1: Define the JMX Exporter Filtering Rules

The Prometheus JMX Exporter allows you to match specific Java Management Extensions (JMX) MBeans and filter out unlisted patterns. We will craft a configuration that exposes *exclusively* your chosen address size and consumer count metrics, leaving behind the remainder of the noise.

Create a local file named `jmx-rules.yaml` containing the configuration below:

[source,yaml]
----
lowercaseOutputName: true
lowercaseOutputLabelNames: true

# Restrict the JMX Agent scanner to the ActiveMQ domain MBeans exclusively
includeObjectNames: ["org.apache.activemq.artemis:*"]

rules:
  # Rule 1: Match Address Size (Gauge)
  - pattern: 'org.apache.activemq.artemis<broker=".*", component=addresses, address="(.*)"><>AddressSize'
    name: artemis_address_size
    type: GAUGE

  # Rule 2: Match Queue Consumer Count (Gauge)
  - pattern: 'org.apache.activemq.artemis<broker=".*", component=addresses, address=".*", subcomponent=queues, routing-type=".*", queue="(.*)"><>ConsumerCount'
    name: artemis_consumer_count
    type: GAUGE

  # Rule 3: Match Queue Delivering Durable Message Count (Gauge)
  - pattern: 'org.apache.activemq.artemis<broker=".*", component=addresses, address=".*", subcomponent=queues, routing-type=".*", queue="(.*)"><>DeliveringDurableMessageCount'
    name: artemis_delivering_durable_message_count
    type: GAUGE
----

== Step 2: Encapsulate the Rules into an OpenShift Secret

To securely pass this customized filtration schema down into your dynamic broker runtime pods, convert the local file into a generic OpenShift secret asset[cite: 1417].

Execute the following command in your terminal:

[source,bash]
----
$ oc create secret generic jmx-metrics-config --from-file=config.yaml=jmx-rules.yaml
----

== Step 3: Author and Deploy the ActiveMQArtemis Custom Resource

We will now modify the core `ActiveMQArtemis` Custom Resource (CR) instance to inject our configuration[cite: 118]. Since the `prometheus.jar` agent is natively baked into the image under `/opt/agents/`, we only need to mount our rules secret via `extraMounts` [cite: 1425] and activate the agent using the `JAVA_ARGS_APPEND` environment variable[cite: 2129].

Create a deployment manifest file named `broker-deployment.yaml`:

[source,yaml]
----
apiVersion: broker.amq.io/v1beta1
kind: ActiveMQArtemis
metadata:
  name: broker-prod
  namespace: amq
spec:
  adminUser: admin
  adminPassword: admin
  version: "7.13" # Match your installed broker minor channel baseline [cite: 47]
  
  acceptors:
    - expose: true
      name: amqp
      port: 5672
      protocols: amqp
      sslEnabled: false # [cite: 2298]

  console:
    expose: true # Exposes the standard management portal endpoint [cite: 2042]

  env:
    # Point directly to the image's pre-installed agent path at /opt/agents/prometheus.jar [cite: 2129]
    - name: JAVA_ARGS_APPEND
      value: '-javaagent:/opt/agents/prometheus.jar=9404:/amq/extra/secrets/jmx-metrics-config/config.yaml'

  deploymentPlan:
    size: 1 # [cite: 648]
    persistenceEnabled: true # [cite: 653]
    journalType: aio # [cite: 3313]
    requireLogin: false # [cite: 650]
    clustered: false
    extraMounts:
      secrets:
        - jmx-metrics-config # Binds our secret to /amq/extra/secrets/jmx-metrics-config/ [cite: 1428, 1430]

  brokerProperties:
    # Opt-out of standard metrics noise via underlying core settings properties [cite: 174]
    - addressSettings.#.enableMetrics=false
    - addressSettings.samplequeue.enableMetrics=true
----

Apply this Custom Resource manifest to your active namespace[cite: 674]:

[source,bash]
----
$ oc apply -f broker-deployment.yaml
----

== Step 4: Verify the Process Runtime Infrastructure

Once the pods transition to a running state[cite: 681], verify that the Java execution command properly contains your agent and config file paths.

Access a remote terminal shell on your running broker pod[cite: 697]:

[source,bash]
----
$ oc rsh broker-prod-ss-0
----

Inside the container shell, verify the JVM process status strings:

[source,bash]
----
sh-5.1$ ps aux | grep java
----

Your process listing command should explicitly display the appended built-in Java Agent parameter pointing to port `9404`:
`... -javaagent:/opt/agents/prometheus.jar=9404:/amq/extra/secrets/jmx-metrics-config/config.yaml ...`

== Step 5: Test and Scrape Custom Telemetry Endpoints

From inside your cluster workspace, query the JMX Exporter metrics collection interface via its designated scraping port to check the output structure:

[source,bash]
----
sh-5.1$ curl localhost:9404/metrics
----

=== Expected Output Payload

Your metric rules output will return highly targeted entries resembling the structured block below:

[source,text]
----
# HELP artemis_address_size the number of estimated bytes being used by all the queue(s) bound to this address
# TYPE artemis_address_size gauge
artemis_address_size{_objectname="org.apache.activemq.artemis<broker=\"amq-broker\", component=addresses, address=\"samplequeue\"><>AddressSize"} 0.0

# HELP artemis_consumer_count number of consumers consuming messages from this queue
# TYPE artemis_consumer_count gauge
artemis_consumer_count{_objectname="org.apache.activemq.artemis<broker=\"amq-broker\", component=addresses, address=\"samplequeue\", subcomponent=queues, routing-type=\"anycast\", queue=\"samplequeue\"><>ConsumerCount"} 0.0
----
